django>=6.0,<6.1
djangorestframework>=3.17,<4.0
djangorestframework-simplejwt>=5.4,<6.0
django-cors-headers>=4.9,<5.0
django-environ>=0.13,<1.0
requests>=2.32,<3.0

# PostgreSQL
psycopg[binary]>=3.2,<4.0

# Static files served directly from gunicorn/wsgi process (no nginx in front).
# brotli extra adds the optional Brotli wheel for better compression than gzip.
whitenoise[brotli]>=6.7,<7.0
